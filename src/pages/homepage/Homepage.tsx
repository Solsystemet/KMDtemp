import { Button } from "../../components/buttons/Buttons";


export function Homepage(){
    return(
        <Button children={undefined} color={""} fontSize={""} onClick={function (): void {
            throw new Error("Function not implemented.");
        } }/>
    )
}